---
apply: always
---

Always run `dotnet build arcane-systems.sln` before finalizing code changes. Treat build failures as blockers.
Make sure the code is C#6 compatible as space engineers uses .net framework 4.8
